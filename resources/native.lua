
-- Mapping of Corona's native library to Marmalade Quick

native = {}

-- Constants --

native.systemFont = "fonts/Default.fnt"
--Native is for Native UI stuff, but Corona can use native fonts
--e.g. systemFont can be used by createText. For now, mapping ot local font fort that use case

return native
